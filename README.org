#+TITLE: tincan.el - a hackish take on agentic coding in Emacs
#+AUTHOR: Marcin 'mbork' Borkowski

This is a small tool to drive Claude Code from Emacs.  It consists of a few parts:
1. A Python script which takes an ongoing conversation with Claude Code and appends it to a plain text file (watchable with =tail -f= or =auto-revert-tail-mode=).
2. A very simple Emacs mode to show that conversation with font-locking.
3. A slightly more complex Emacs mode to gather the user's responses and send them back to Claude Code.
4. A set of Claude Code hooks to tell Emacs whether it's working or idle.
5. Emacs commands orchestrating all previous parts.

All five parts are implemented: the transcript script, the font-locked viewer,
the working/idle and needs-input signals, the input mode (compose replies and
send them), and the commands that tie them together.  Claude runs in an Emacs
=vterm= terminal (no tmux) with a linked, read-only rendered view alongside it.

* Why both the view and the terminal

# (Terse draft notes; expand into prose.)

- Faithful history
  - from the transcript (JSONL), not the terminal repaint
  - immune to repaint bugs (duplicated/missing lines)
  - main motivation
- Comfortable reply composition
  - dedicated buffer, not the TUI line editor
  - editing directly in vterm/term is awkward
- Free view bonuses: folding, search, rendering, navigation
- Asymmetry between the two surfaces
  - terminal = ground truth + sole input channel
  - view = faithful, navigable reading
  - neither replaces the other
- Caveat: the view is faithful only to what reaches the JSONL
  - TUI-only content absent (e.g. permission prompts)
  - hence the separate "needs-input" hack
- tincan.py stands alone
  - tincan.py + tail -f useful for non-Emacs users too
- Claude Code ToS conformance
  - advantage over tools like agent-shell
  - real Claude Code in a terminal, not a separate API client
  - the official CLI drives Claude

* Input mode - usage

# (Terse draft notes; expand into prose.)

Commands:
- =M-x tincan= (=tincan-dwim=) - front door, current project: switch to a live
  session if one is open, else resume the most recent, else start a new one
- =M-x tincan-start= - always a NEW session in =default-directory=: generates an
  id, runs =claude --session-id <id>=, shows the terminal, view follows behind.
  Outside a project root (=project-current=), confirmation first - usually a
  stale =cd=.  Same prompt on =tincan='s start-a-new branch
- =M-x tincan-resume= - resume an existing session, picked from a list; relaunches
  =claude --resume <id>= in the session's own recorded directory; shows the view,
  terminal buried.  The prefix widens the list in notches (each =C-u= shows more):
  none = this project, last =tincan-recent-days= (7) days; =C-u= = this project,
  all history; =C-u C-u= = all projects, last N days; =C-u C-u C-u= = all
  projects, all history
- =M-x tincan-view= - observe a session read-only (no terminal, no replies); same
  prefix widening as =tincan-resume=
- =M-x tincan-attach= - from a terminal buffer, (re)build and link its view
- =M-x tincan-rename= - rename the view and terminal buffers to the session's
  current title (picks up a =/rename=)

Keys - view:
- =C-c SPC= / =r= - reply (compose buffer; jumps to the terminal when Claude waits)
- =C-c o= / =t= - go to the terminal
- =C-c k= - close the session (ends Claude in the terminal)
- =C-c 0= - delete the terminal window
- =q= - bury the view
- =SPC= / =DEL= - page down / up; =<= / =>= - buffer ends (line motion on =C-n= /
  =C-p= and the arrows)
- navigation, finest to coarsest: =n= / =p= / =u= - next / previous / up over any
  heading (@@@ marker or Markdown heading; =u= climbs to the enclosing @@@);
  =M-n= / =M-p= - only @@@ section markers; =[= / =]= - previous / next
  USER / ASSISTANT turn
- =TAB= - fold the section at point (from its heading or its body); =S-TAB= -
  whole buffer
- =c= - hide all sections except USER / ASSISTANT (toggle; navigation skips them)
- =e= - restore emphasis fontification after the long-line guard disabled it
  (header shows =[no emphasis]=; =C-u e= forces mid-turn)
- =/= - search (=isearch-forward=); =w= - copy the section (or code block) at point
- =RET= - visit the file at point; =?= - describe the mode

Keys - terminal:
- =C-c SPC= - reply; =C-c o= - go to the view; =C-c k= - close the session
- =C-c C-c= - send a real interrupt (C-c) to Claude
- =S-RET= - newline in Claude's prompt without submitting (needs kkp on a tty)
- =C-z= - disabled (would suspend the TUI)

Compose buffer:
- =C-c C-c= - send to the terminal (bracketed paste + Return); confirms first if
  Claude is still working
- =C-c C-k= - cancel (confirms before discarding a non-empty draft)
- =C-c C-z= - hide the compose buffer, keeping the draft; reply again to restore it
- unsent draft kept across Emacs exits; restored on the next reply to that session

Options:
- =tincan-claude-command= - the program (default =claude=)
- =tincan-show-terminal-on-send= - =display= / =select= / =none= (default =display=)
- =tincan-dismiss-terminal-on-next-command= - the next view command dismisses the
  peeked terminal window (default =t=)
- =tincan-send-return-delay= - seconds between paste and Return (default =0.1=;
  raise if replies do not submit)
- =tincan-terminal-scroll-margin= - bottom margin so prompts stay on-screen (default =8=)
- =tincan-buffer-title-width= - title width in buffer names (default =16=)
- =tincan-long-line-threshold= - line length that switches off emphasis
  fontification in the view (default =2000=; =nil= disables the guard)
- =tincan-persist-drafts= - unsent drafts survive Emacs exits, in
  =~/.claude/tincan/<session-id>.draft= (default =t=)

Requirements and notes:
- vterm needed for the terminal (without it: read-only =tincan-view= only)
- =markdown-ts-mode= (Emacs 31) or =gfm-mode= improves rendering
- buffers named by title: =*tincan view: <title>*= / =*tincan terminal: <title>*=
  (short id until Claude sets one; =tincan-rename= refreshes both)
- flow: view -> =C-c SPC= reply -> compose -> =C-c C-c= -> terminal -> =C-c o= back

* Usage

Requirements: Python 3 (standard library only) for the script, and Emacs 30.1+
for the viewer.  For the nicest rendering, install [[https://github.com/jrblevin/markdown-mode][markdown-mode]] (optional).

** Selecting a session

=tincan.py= reads Claude Code's own transcript files under
=~/.claude/projects/=.  Run it from your project (or any subdirectory) to list
that project's sessions, most recently active first; add =--all= for every project:

#+begin_src sh
./tincan.py --show-sessions          # sessions of the enclosing project
./tincan.py --show-sessions --all    # every project's sessions
#+end_src

Each line is =SESSION-ID<TAB>TIMESTAMP<TAB>TITLE<TAB>CWD=, where =TIMESTAMP= is
the time of the session's most recent activity (not its creation) and is also
the sort key.  Without =--all= it lists the sessions of the closest launch
directory at or above the working directory.  A session can be named by its id,
a unique id prefix, or a path to a =.jsonl= transcript.

** Following a transcript with =tail -f=

Print a whole transcript to standard output:

#+begin_src sh
./tincan.py SESSION
#+end_src

Or keep watching a live session and append new turns as they arrive, just like
=tail -f=.  The script only ever appends to stdout and tolerates the partially
written, invalid JSON of an in-progress turn:

#+begin_src sh
./tincan.py SESSION --follow
#+end_src

To watch with =tail -f= itself, stream the transcript into a file and follow
that file:

#+begin_src sh
./tincan.py SESSION --follow > transcript.md &
tail -f transcript.md
#+end_src

** Viewing in Emacs

Load the mode:

#+begin_src emacs-lisp
(add-to-list 'load-path "/path/to/tincan.el")
(require 'tincan)
#+end_src

Then observe a session live, read-only, with one command:

#+begin_src
M-x tincan-view
#+end_src

Run it from the project (any subdirectory works).  It lists that project's
recent sessions (last =tincan-recent-days= days), lets you pick one, launches
=tincan.py --follow=, and shows the rendered transcript in a read-only buffer
that updates as Claude works.  The prefix argument widens the list in notches
(each =C-u= shows more: this project all history, then all projects) - narrow by
title or directory once broadened.  To drive Claude rather than only watch, use
=M-x tincan= (the front door) - see
[[*Input mode - usage][Input mode - usage]] above.

Narrowing by directory in that picker depends on your completion setup: Ivy,
Helm and Vertico (with =orderless=) match anywhere out of the box, while vanilla
=completing-read= wants =flex= or =substring= in =completion-styles= to match a
directory in the middle of a candidate.

The header line shows the agent's state: =[working]=, =[idle]= (after a =@@@ DONE=
turn-end marker, derived from Claude Code's =turn_duration= record), and - if the
optional hook below is installed - =[needs input]= when Claude is waiting for you
in the terminal.

=M-x tincan-view= renders through =tincan-render-buffer=, which uses =markdown-mode=
(or =gfm-mode=) when available - including native, per-language fontification of
fenced code blocks - and otherwise falls back to the plain =tincan-view-mode=.
Either way the =@@@ ROLE= section markers (=USER=, =ASSISTANT=, =THINKING=,
=TOOL_USE=, =TOOL_RESULT=, and =DONE=) are font-locked and the buffer is
read-only.

Two tool calls get a shape of their own instead of raw JSON, path on the marker
line: =Write= - file content, language from the extension; =Edit= - unified diff
of the replaced snippet, fontified by =diff-mode=.  Line numbers relative to the
snippet, so readable but not appliable.

The sections are foldable: =TAB= cycles a section's visibility - on its =@@@=
heading or from anywhere inside its body - and =S-TAB= cycles the whole buffer.
Every section except =USER= and
=ASSISTANT= starts folded - so thinking, tool calls, tool results and =DONE=
markers stay out of the way until you open them, including as they stream in.
Customize which roles stay open with =tincan-unfolded-sections=.

Markdown headings inside messages fold too: =TAB= on a Markdown heading
(e.g. =##=) folds that heading's subtree; headings nest below the =@@@= sections.

Markdown tables are aligned automatically as sections stream in (via
=markdown-table-align=, so alignment colons and Unicode widths are respected);
this needs a Markdown mode - it is a no-op in the =tincan-view-mode= fallback.
=M-x tincan-align-tables= re-aligns the whole buffer on demand.

To render a static transcript file instead (for example one you streamed to disk
with =--follow > file=), open it and call =tincan-render-buffer= yourself,
optionally with =auto-revert-tail-mode= for tail-following:

#+begin_src emacs-lisp
(progn
  (find-file "/path/to/transcript.md")
  (tincan-render-buffer)
  (auto-revert-tail-mode))
#+end_src

Relevant options (=M-x customize-group RET tincan RET=):
- =tincan-markdown-mode= - which Markdown mode to use, or =nil= to disable it.
- =tincan-fontify-code-blocks-natively= - per-language code-block highlighting.
- =tincan-format-tables= - auto-align Markdown tables as they stream in.

** Knowing when Claude wants your input (optional)

When Claude pauses for a tool-permission prompt (or sits idle waiting for a
prompt), nothing is written to the transcript - that prompt is terminal-only
UI - so the viewer cannot show it on its own.  To surface it, tincan can install
an optional Claude Code =Notification= hook that writes a small per-session file
at =<config-dir>/tincan/<session-id>.notify=; the view watches that file and
shows =[needs input]= in the header line.  The hook only signals that Claude
wants you - you still answer the prompt in the terminal.

Install (and uninstall) it from Emacs:

#+begin_src emacs-lisp
(tincan-install-hook)    ; M-x tincan-install-hook
(tincan-hook-installed-p)
(tincan-uninstall-hook)
#+end_src

These are thin wrappers around the script, which does the actual work and can
also be driven directly:

#+begin_src sh
./tincan.py --install-hook      # also --uninstall-hook, --check-hook
#+end_src

By default the script edits =.claude/settings.local.json= in the current
directory - your personal, gitignored project settings, so the hook fires only
for this project's sessions.  Run install from where you start Claude Code, or
override with =--settings-file= (or, in Emacs, =tincan-hook-settings-file=).  It
backs the file up to =settings.local.json.bak= and keeps any existing settings
and hooks.  Because Claude Code reviews hook changes for safety, restart it or
run =/hooks= afterwards so the hook is loaded.
